node_modules/
.next/
out/
build/
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts
__pycache__/
*.py[cod]

# The reference project this design was studied from. Kept locally, not vendored:
# it has its own git history and its own author.
refrance-extract-useful-things/

# NOT ignored, on purpose:
#   data/*.csv            the evidence ledger — reviewable, and hashes must be stable
#   data/decision_log.json  what the agent claimed and what each row hashed to
#   evals/report.md         the evaluation write-up
#   evals/raw/*.json        raw outputs, so every number in the README is checkable
